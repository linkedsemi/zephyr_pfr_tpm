sample:
  description: .. spi_monitor_warm_reset
    application
  name: spi_monitor_warm_reset
common:
  tags: introduction
  integration_platforms:
    - native_sim
  harness: console
  harness_config:
    type: one_line
    regex:
      - "spi_monitor_warm_reset! (.*)"
tests:
  sample.basic.helloworld:
    tags: introduction
